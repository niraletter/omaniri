#!/bin/bash

# omaniri:summary=Reboot after closing application windows
# omaniri:examples=omaniri reboot | omaniri system reboot
# omaniri:aliases=omaniri reboot

# Schedule the reboot in the user manager so closing the terminal's systemd
# scope cannot terminate it before it runs.
systemd-run --user --collect --quiet --on-active="2s" --timer-property=AccuracySec=100ms systemctl reboot --no-wall || exit 1

omaniri-osd -i reboot -m "Rebooting" -d 5000

omaniri-state clear re*-required

# Now close all windows
omaniri-hyprland-window-close-all
sleep 1 # Allow apps like Chrome to shutdown correctly
