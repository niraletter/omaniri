#!/bin/bash

# omaniri:summary=Remove Steam and all of its game libraries, configs, and caches.
# omaniri:requires-sudo=true

set -e

omaniri-pkg-drop steam

rm -rf \
  "$HOME/.steam" \
  "$HOME/.local/share/Steam" \
  "$HOME/.config/steam" \
  "$HOME/.cache/steam"

echo ""
echo "Steam and its data have been removed."
