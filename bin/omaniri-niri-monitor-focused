#!/bin/bash

# omaniri:summary=Print the name of the currently focused monitor.
# omaniri:group=niri

niri msg -j focused-output 2>/dev/null | jq -r '
  if .Ok then
    (.Ok | if type == "object" then . else .[0] end) | .name // empty
  else empty end
'
