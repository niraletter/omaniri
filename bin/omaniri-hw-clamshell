#!/bin/bash

# omaniri:summary=Returns true when clamshell mode is active
# omaniri:hidden=true

# Clamshell = lid closed while driving one or more external monitors.
omaniri-hw-laptop-closed && omaniri-hw-external-monitors
